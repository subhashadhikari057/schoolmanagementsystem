import React from 'react';

export default function Change({
  value,
  isPositive = true,
  className,
}: {
  value: string | number;
  isPositive?: boolean;
  className?: string;
}) {
  return null;
}
